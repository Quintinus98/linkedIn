import React from "react";
import Button from "../button";

const AddFeed = () => {
  return (
  <div className="container">
    <Button icon={<i className="fa-solid fa-plus"></i>} text="Follow"/>
  </div>
  );
};

export default AddFeed;
